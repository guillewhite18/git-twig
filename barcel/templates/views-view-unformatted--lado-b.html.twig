
 	 {{base_url}}   
 
 	{% set theme_url = drupal_get_path('theme','barcel') %}
 	{% set theme_base = base_url~'/'~theme_url~'/' %}

 	/*** Get the vid ***/
	{% set nid = view.render_field('nid',0) %}
    {% set node = node_load(nid) %}
    {% set vid = node.field_video'und'.0.video_id %}


<!-- Section content Lado B video -->
<section id="lado-video-section" class="section-container">
	<div class="container lado-video-container">
		<div class="embed-responsive embed-responsive-16by9">
		 <iframe class="embed-responsive-item" src="https://www.youtube.com/embed/{{ vid }}" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
		</div>
	</div>
</section>